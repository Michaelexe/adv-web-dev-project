# Example environment variables for the Flask backend
# Use a proper Postgres URI in production, for example:
# DATABASE_URL=postgresql://username:password@localhost:5432/dbname

# If not provided, the app will fall back to sqlite:///dev.db
DATABASE_URL=

# Secret key used by Flask; set a secure value in production
SECRET_KEY=change-me-to-a-secure-random-value
